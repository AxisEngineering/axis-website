# Private working notes — never publish
*.docx

# Codex / audit reports & artefacts — internal only, never publish
# Broad pattern: NO internal Axis_Website_*.txt report should ever be committed
Axis_Website_*.txt
audit-artifacts/

# Build artefacts & OS junk
*.zip
.DS_Store
Thumbs.db
